# `src/assets`

Esta carpeta se conserva para cumplir la arquitectura solicitada.

Durante la modernización se eliminaron duplicados no usados desde `src/assets` para reducir mantenimiento.

Los assets runtime activos (imágenes, audio y `theme-init.js`) viven en `public/assets` para rutas estables del estilo original (`/assets/...`).
